fn main() {
    let i = 0;
    let list = [];

    while i < 10 {
        list.push(i);
        i += 1;
    };

    println(list);

    let i = 0;
    
    while i < list.len() {
        list[i] = list[i] * 2;
        i += 1;
    };

    println(list);
}

disassemble(main);
main();
